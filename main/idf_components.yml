dependencies:
  idf: ">=5.0"

  # --- Сеть и Wi-Fi ---
  esp_wifi: "*"
  esp_netif: "*"
  esp_event: "*"
  nvs_flash: "*"

  # --- MQTT ---
  mqtt: "*"

  # --- Логгирование и системные функции ---
  log: "*"
  freertos: "*"
  esp_timer: "*"
  esp_system: "*"

  # --- UART / GPIO / SPI / I2C / и т.д. ---
  driver: "*"

  # --- TLS / HTTPS / OTA (если понадобится) ---
  esp_tls: "*"
  esp_http_client: "*"
  esp_https_ota: "*"

  # --- Тестирование и отладка (опционально) ---
  unity: "*"
  console: "*"
  esp_rom: "*"